anthropic>=0.40.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-docx>=1.1.0
# Optional: enables real BPE token counting offline (no API key). The optimizer
# falls back to a char/word heuristic if this isn't installed.
tiktoken>=0.7.0
